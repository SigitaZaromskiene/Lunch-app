import logo from "../../assets/img/logo.svg"

const LogoHorizontal: React.FC = () => {
  return  <img src={logo} className='logo' alt="Logo of the page with app name and food bowl" />
};

export default LogoHorizontal;
